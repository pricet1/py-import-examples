# utils/__init__.py

# Absolute imports (better choice than relative imports as less prone to
# breaking): specify the absolute path of the imported module from the project
# root (or any other dir which sys.path has access to)

from utils.lower import to_lower
from utils.upper import to_upper
from utils.length import get_length
